# Pinned to compatible ranges to keep the double-click workflow stable across
# reinstalls. Bump deliberately, not incidentally. (plotly removed — unused.)
pandas>=2.2,<4
openpyxl>=3.1,<4
requests>=2.32,<3
Django>=5.2,<6
gunicorn>=23,<24
whitenoise>=6.9,<7
# Cloud-only: S3-compatible durable DB store (Cloudflare R2 / S3 / B2). Lazily
# imported — unused and harmless in the local double-click workflow.
boto3>=1.34,<2
